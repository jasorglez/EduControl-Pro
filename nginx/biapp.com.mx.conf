server {
    listen 80;
    server_name biapp.com.mx www.biapp.com.mx;

    # Redirigir HTTP → HTTPS (descomentar después de configurar SSL con certbot)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── HTTPS (agregar después de: sudo certbot --nginx -d biapp.com.mx) ──────────
# server {
#     listen 443 ssl;
#     server_name biapp.com.mx www.biapp.com.mx;
#
#     ssl_certificate     /etc/letsencrypt/live/biapp.com.mx/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/biapp.com.mx/privkey.pem;
#
#     location / {
#         proxy_pass         http://localhost:3000;
#         proxy_http_version 1.1;
#         proxy_set_header   Upgrade $http_upgrade;
#         proxy_set_header   Connection 'upgrade';
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
